services:
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: mern-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGODB_URI=mongodb+srv://ptcptlst01:01689868380Cong@cluster0.n6uoeez.mongodb.net/cloud-lab?appName=Cluster0&retryWrites=true&w=majority
    networks:
      - mern-network
    restart: always

  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: mern-frontend
    ports:
      - "4173:4173"
    depends_on:
      - backend
    networks:
      - mern-network
    restart: always

networks:
  mern-network:
    external: true